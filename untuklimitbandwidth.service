[Unit]
Description=Bandwidth shaper/Network rate limiter
After=network-online.target
Wants=network.target

[Service]
Type=oneshot
RemainAfterExit=yes
EnvironmentFile=/etc/systemd/untuklimitbandwidth.conf
ExecStart=/usr/sbin/untuklimitbandwidth -a $IFACE -d $DSPEED -u $USPEED
ExecStop=/usr/sbin/untuklimitbandwidth -c -a $IFACE

[Install]
WantedBy=multi-user.target
